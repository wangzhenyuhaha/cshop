<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="41dp">

    <RelativeLayout
        android:id="@+id/ll_tab_title"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center">

        <TextView
            android:id="@+id/tvTabTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:layout_centerVertical="true"
            android:textColor="#ff000000"
            android:textSize="14sp"
            android:lines="1"
            tools:text="待发货" />

        <TextView
            android:lines="1"
            android:text="sdfsdf"
            android:id="@+id/tvTabHint"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:layout_centerVertical="true"
            android:layout_toRightOf="@+id/tvTabTitle"
            android:textColor="#ff000000"
            android:textSize="14sp" />
    </RelativeLayout>

    <View
        app:layout_constraintLeft_toLeftOf="@id/ll_tab_title"
        app:layout_constraintRight_toRightOf="@id/ll_tab_title"
        app:layout_constraintBottom_toBottomOf="parent"
        android:id="@+id/viTabLine"
        app:layout_constraintWidth_default="percent"
        app:layout_constraintWidth_percent="0.6"
        android:layout_marginLeft="@dimen/dimen_20"
        android:layout_marginRight="@dimen/dimen_20"
        android:layout_height="2dp"
        android:layout_width="0dp"
        android:layout_alignParentBottom="true"
        android:background="#3870EA" />
</androidx.constraintlayout.widget.ConstraintLayout>